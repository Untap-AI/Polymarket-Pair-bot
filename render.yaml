# render.yaml — Render Blueprint (Infrastructure-as-Code)
#
# To deploy: connect this repo on Render and it will auto-detect this file.
# Set the secret environment variables in the Render dashboard after creation.

services:
  # Daily parameter optimization reports (5am PST / 13:00 UTC) — one per asset
  - type: cron
    name: daily-report-btc
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: python scripts/daily_report.py --markets BTC
    schedule: "0 13 * * *"
    region: frankfurt
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_EMAIL
        sync: false
      - key: SENDGRID_TO_EMAIL
        sync: false

  - type: cron
    name: daily-report-eth
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: python scripts/daily_report.py --markets ETH
    schedule: "0 13 * * *"
    region: frankfurt
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_EMAIL
        sync: false
      - key: SENDGRID_TO_EMAIL
        sync: false

  - type: cron
    name: daily-report-sol
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: python scripts/daily_report.py --markets SOL
    schedule: "0 13 * * *"
    region: frankfurt
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_EMAIL
        sync: false
      - key: SENDGRID_TO_EMAIL
        sync: false

  - type: cron
    name: daily-report-xrp
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: python scripts/daily_report.py --markets XRP
    schedule: "0 13 * * *"
    region: frankfurt
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: SENDGRID_FROM_EMAIL
        sync: false
      - key: SENDGRID_TO_EMAIL
        sync: false
